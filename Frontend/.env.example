# Populate these once your Supabase project is ready, then flip
# USE_MOCK to false in src/lib/api.js — every function there already
# mirrors the query it will become.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:8000
